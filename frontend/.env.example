# URL absoluta de la API (cuando NO usas el proxy de Vite, por ejemplo en producción)
# VITE_API_URL=http://localhost:8080/api

# Target del proxy de desarrollo de Vite. Si está definido, las peticiones a /api
# en `npm run dev` se redirigen aquí.
VITE_API_PROXY_TARGET=http://localhost:8080
